# MultiStringMultiFileSearcher
Simple console application to take an input file with a list of search strings, and search all files under a directory for any of those search strings.  Output saved to csv.
